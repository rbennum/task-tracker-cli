[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "task-cli"
version = "0.0.1"
description = "A simple CLI task tracker"
authors = [
    {name = "Bening Ranum"}
]
dependencies = [
    "platformdirs",
]

[project.scripts]
task-cli = "task_cli.main:main"
